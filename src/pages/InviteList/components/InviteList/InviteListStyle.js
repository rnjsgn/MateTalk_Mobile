import { StyleSheet } from "react-native";

export const InviteListStyle = StyleSheet.create({
    container : {
        width : '100%',

        alignItems : 'center'
    },

    title : {
        margin : 15,

        fontSize : 24,
        fontWeight : 'bold'

    }
})